#include <stdio.h>

int main(){
	unsigned int n;
	printf ("nhap so luong phan tu cua day so: ");
	scanf("%u",&n);
	float a[n]=1;
		priintf("\nNhap phan tu thu %u: ";
	for (inti<=n;i++){ );
		scanf("%f", &a[i]);
	}
	printf("\n");
	for (int i=1; i<=n;i++){
		printf("%.2f ",a[i]);
	}
return 0;
}
